language: python

python:
  - 3.6

cache:
  pip: true
  directories:
    - node_modules

#install:
#  - yarn add serverless

jobs:
  include:
    - stage: test
      script: python -m unittest
    - stage: deploy
      language: node_js
      node_js:
        - 8
      install:
        - yarn add serverless
      script: yarn run serverless deploy --stage dev